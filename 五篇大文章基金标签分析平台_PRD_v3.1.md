# 五篇大文章基金标签分析平台 PRD

> 版本：v3.1（优化基金详情基本信息布局）  
> 文档状态：可用于重新构建完整静态网站  
> 工作名称：五篇大文章基金标签分析平台  
> 项目正式名称：基金池“五篇大文章”标记体系  
> 课题组或单位名称：中银理财-基海淘金队  
> 当前数据统计日期：截至2026年9月30日

## 1. 产品概述

### 1.1 项目背景

课题的研究流程为“股票池打标—基金池打标—展示基金池打标结果”。网页首版不展示股票池打标方法、基金持仓汇总过程或阈值推导过程，只呈现基金池最终标签结果。

正式数据源已确定为项目根目录下的 `fund_labels_web_cleaned.xlsx`。项目采用“人工替换数据源的离线批量更新”方式，由维护人员按季度使用经过确认的新 Excel 覆盖该同名文件，再运行一次统一构建命令，自动转换为网页读取的标准 JSON 并重新生成静态网站。该流程不改变页面结构与核心代码。数据适配层必须按字段名读取所需数据，不得依赖 Excel 中固定的列序号；正式数据增加、减少或调整不参与页面逻辑的冗余列时，不应导致导入失败。

### 1.2 产品定位

面向课题评审、领导汇报和后续成果展示的桌面端基金标签分析工具。产品以基金池检索、标签组合筛选和单只基金五维画像为核心，整体呈现为简洁、现代、专业的数据分析产品。首版采用无需后端服务的纯前端静态单页应用，构建结果可部署到普通静态托管环境或单位内部静态服务器。

### 1.3 产品目标

1. 支持使用单个或多个基金代码构建待查看基金子集，并在列表中集中展示匹配结果。
2. 支持按二级分类、核心标签、关联标签及多个条件组合进一步筛选基金池。
3. 通过雷达图和比例条形图直观展示单只基金的五维标签画像。
4. 建立稳定的 Excel → 标准 JSON 数据协议，后续仅替换同名正式数据文件即可更新结果。
5. 在普通桌面浏览器、会议室电脑和大屏上保持清晰、稳定的展示效果。

### 1.4 非目标（首版不做）

- 不展示股票池打标规则、模型推导过程或底层持仓证据。
- 不提供标签阈值在线编辑功能。
- 不提供面向普通用户的网页内 Excel 上传后台。
- 不提供基金名称搜索。
- 不提供结果导出、基金收藏或多基金对比。
- 不提供用户登录、权限体系和操作日志。
- 不以手机端为首版适配目标。
- 不直接连接正式数据库或内部 API。

## 2. 用户与使用场景

### 2.1 主要用户

- 课题评审人员
- 领导及成果汇报对象
- 后续公开展示页面的访问者（公开范围待确定）

### 2.2 核心场景

1. 汇报人员在会议室电脑或大屏上演示基金标签结果。
2. 用户输入一个或多个完整基金代码，在基金列表中集中查看匹配基金。
3. 用户选择一个或多个核心/关联标签，筛选符合条件的基金。
4. 用户点击基金池列表中的基金代码或基金简称链接，进入基金详情页查看五维画像。
5. 用户在基金详情页输入另一只完整基金代码，直接切换至该基金的五维画像。

两条核心使用路径：

- 列表筛选：输入一个或多个基金代码＋可选标签条件 → 基金列表 → 点击基金代码或基金简称 → 基金详情页。
- 详情切换：在基金详情页输入一个完整基金代码 → 切换至该基金详情。

## 3. 信息架构

首版采用两页结构：

```text
基金池页
├── 项目身份区
├── 基金池概览区
│   ├── 四项全量基金指标
│   └── 五类标签覆盖基金数横向条形图
├── 单个/批量基金代码筛选
├── 二级分类筛选
├── 核心标签筛选
├── 关联标签筛选
└── 基金标签结果表
     └── 点击基金代码或简称 → 基金详情页
                         ├── 单只基金代码搜索/切换
                         ├── 基金基本信息卡片
                         ├── 核心/关联标签摘要
                         ├── 五维标签雷达图
                         └── 五类标签占比条形图
```

### 3.1 路由建议

- 首版默认使用 React Router `HashRouter`，避免静态托管环境缺少 SPA 路由回退配置时刷新详情页失败。
- 应用内部基金池路由：`/funds`；浏览器地址示例：`/#/funds`。
- 应用内部基金详情路由：`/funds/:fundCode`；浏览器地址示例：`/#/funds/AAA.OF`。
- 根路由 `/` 自动进入 `/funds`。
- 页面组件和 `Link` 只使用应用内部路由，不在业务组件中手工拼接 `#`。

## 4. 标签体系与业务规则

### 4.1 五个正式标签

1. 科技金融
2. 绿色金融
3. 普惠金融
4. 养老金融
5. 数字金融

### 4.2 当前分类假设

- 科技金融、绿色金融、普惠金融、养老金融、数字金融是五个相互独立的标签维度，不是互斥分类。
- 同一基金可以同时满足多个标签条件；当前正式数据中核心标签可能有一个或两个，页面与内部数据结构不得假设核心标签唯一。
- 五类占比不要求合计为 100%，合计值也允许超过 100%。
- 数据校验只检查每个标签占比是否分别位于 0–100% 区间，不对五类占比之和设置上下限。
- 雷达图和横向条形图分别展示五个独立维度，不得使用饼图或“剩余未归类占比”表达。

### 4.3 核心标签与关联标签

对基金的每一个五篇大文章标签占比独立判断：

| 标签身份 | 判定区间 | 页面表现 |
| --- | ---: | --- |
| 核心标签 | 占比 ≥ 50% | 高强调标签，展示准确百分比 |
| 关联标签 | 20% ≤ 占比 < 50% | 次级强调标签，展示准确百分比 |
| 未入选标签 | 占比 < 20% | 不进入核心/关联摘要，但仍在雷达图和条形图展示 |

边界规则：

- 50% 归入核心标签，不再归入关联标签。
- 20% 归入关联标签。
- 若所有标签均低于 50%，核心标签显示“暂无核心标签”。
- 若一个或多个标签同时达到 50%，均进入核心标签数组，不进行任意择一。
- 五类占比字段为空、`NA`、`N/A`或`--`时，统一按 0 处理，并以 0 参与标签判定和图表绘制。
- 上述缺失值转 0 规则仅适用于五类标签占比，不适用于基金简称、管理人、基金分类等文本字段。

### 4.4 规则实现位置

推荐保留五类原始占比，并在独立的数据适配/业务规则层统一计算 `coreLabels` 与 `relatedLabels`；页面组件只消费计算结果，不在各组件内部重复编写阈值判断。

如果正式 Excel 已包含核心与关联标签字段，系统仍应使用五类占比重新计算并进行一致性核对。出现差异时，首版以数据适配层的重新计算结果为准，并在开发控制台记录基金代码、源表结果和重新计算结果。网页内部的标签集合采用动态数组，不受源表中“关联标签1—3”等固定槽位数量限制。

## 5. 页面一：基金池页

### 5.1 页面目标

允许用户通过单个或多个基金代码及标签条件筛选基金池，并通过列表中的基金代码或基金简称链接进入单只基金详情。

### 5.2 页面布局

桌面端建议采用最大宽度 1440px 的居中内容区：

1. 顶部项目身份区
2. 基金池概览区
3. 基金代码筛选区
4. 二级分类与标签筛选区
5. 当前筛选条件与结果数量
6. 基金结果表

不建议将基金代码输入区铺满整个大屏；建议宽度约 560–720px，并允许随多行输入适度增高。

### 5.3 顶部项目身份区

必须展示：

- 项目正式名称：`基金池“五篇大文章”标记体系`
- 课题组或单位名称：`中银理财-基海淘金队`
- 数据统计日期：`截至2026年9月30日`

不要求展示单位 Logo。

展示规则：

- 项目名称位于左侧，课题组名称与数据统计日期位于右侧或标题下方的次级信息区。
- 数据统计日期从标准 JSON 的 `meta.asOfDate` 读取并格式化为“截至YYYY年M月D日”，不得在多个页面组件中分别硬编码。
- 基金池页和基金详情页必须显示相同的数据统计日期。

### 5.4 基金池概览区

概览区位于项目身份区下方、搜索与筛选区域上方，用于在进入页面时先展示整个基金池的标签结果。无需为概览区单独增加首页。

概览区左侧或上方展示四个紧凑指标卡：

| 指标 | 统计定义 |
| --- | --- |
| 基金总数 | 全量有效基金记录数 |
| 具有核心标签的基金数 | `coreLabels.length > 0` 的基金数，包括同时具有核心和关联标签的基金 |
| 仅有关联标签的基金数 | `coreLabels.length = 0` 且 `relatedLabels.length > 0` 的基金数 |
| 暂无核心或关联标签的基金数 | `coreLabels.length = 0` 且 `relatedLabels.length = 0` 的基金数；卡片内增加口径备注“纯债型基金不涉及相关标签” |

四项指标的后三项应互斥并覆盖全部基金，即“具有核心标签的基金数＋仅有关联标签的基金数＋暂无核心或关联标签的基金数＝基金总数”。

“暂无核心或关联标签的基金数”指标卡展示规则：

- 在指标数值下方常驻显示备注：`纯债型基金不涉及相关标签`。
- 备注使用中性灰色次级文字，建议字号12–13px、行高1.4，不使用主题强调色，不采用悬浮提示。
- 备注与指标数值之间保留8–12px间距；文字较长时允许自然换行，但不得挤压或遮挡指标数值。
- 该备注仅用于解释业务口径，不参与数据判断，也不改变“暂无核心或关联标签的基金数”的现有统计条件。

概览区右侧或下方展示“五类标签覆盖基金数”横向堆叠条形图：

- 纵轴固定为科技金融、绿色金融、普惠金融、养老金融、数字金融。
- 每个标签使用“核心标签覆盖基金数”和“关联标签覆盖基金数”两段堆叠展示。
- 条形末端显示该标签的总覆盖基金数，即核心覆盖数与关联覆盖数之和。
- 悬浮提示分别展示核心覆盖数、关联覆盖数和总覆盖数。
- 五类标签沿用全站固定颜色；核心部分使用较深色，关联部分使用同色系浅色。
- 概览指标和覆盖图均根据全量基金池计算，不随基金代码筛选或标签筛选条件变化。
- 五类标签独立统计，同一基金可能同时计入多个标签的覆盖基金数；不同标签的覆盖基金数不可直接相加或与基金总数进行加总核对。

### 5.5 基金代码筛选

- 仅支持基金代码精确匹配，不支持基金简称搜索、代码模糊搜索或搜索联想。
- 支持输入单个基金代码，也支持一次输入多个基金代码形成待查看基金子集。
- 多个代码可使用英文逗号、中文逗号、英文分号、中文分号、空格或换行分隔，便于从 Excel 直接粘贴。
- 用户点击“筛选”按钮或按 `Ctrl+Enter`（macOS为 `Command+Enter`）后执行解析和筛选；普通 Enter 用于输入换行。筛选结果仍在当前基金结果表中展示，不直接跳转至基金详情页。
- 对每个输入项依次执行 `trim().toUpperCase()`，移除空项并去重，再与全量基金数据中的 `fundCode` 精确匹配。
- 不自动补全 `.OF` 后缀；用户必须输入数据中存在的完整基金代码。
- 已匹配基金继续正常展示；部分代码未匹配时不得使整次筛选失败。
- 表格上方显示：`输入N只基金，成功匹配M只，当前条件下显示K只`。
- 未匹配代码单独提示，例如：`未找到：AAA.OF、BBB.OF，请检查输入是否正确。`
- 基金代码筛选与二级分类、核心标签、关联标签筛选采用“且”关系，即先限定输入的基金子集，再在该子集中应用其他条件。
- 清除基金代码条件后，恢复为仅受当前核心/关联标签条件约束的基金结果。
- 执行新的基金代码筛选时，页码重置为第1页；不会改变基金池概览区的全量统计口径。
- 当前代码条件应作为可识别、可清除的筛选状态保留；从详情页返回基金池时需恢复原输入内容和解析结果。

输入提示文案：`输入一个或多个完整基金代码，可用逗号、空格或换行分隔`

### 5.6 二级分类与标签筛选器

**二级分类**

- 采用带复选框的多选下拉筛选器，选项由全量基金数据中的 `secondaryCategory` 去重后动态生成，不在前端硬编码。
- 选项按中文名称升序排列，并显示已选择数量。
- 同时选择多个二级分类时采用“或”逻辑。
- 二级分类筛选与基金代码、核心标签、关联标签筛选之间采用“且”逻辑。
- 当二级分类选项较多时，下拉面板内部允许滚动；首版不增加二级分类文本搜索。

**标签筛选器**

为避免“先选择核心/关联身份，再选择标签”产生状态歧义，建议直接设置两组并列筛选器：

**核心标签**

- 科技金融
- 绿色金融
- 普惠金融
- 养老金融
- 数字金融

**关联标签**

- 科技金融
- 绿色金融
- 普惠金融
- 养老金融
- 数字金融

交互规则：

- 每组均支持多选。
- 同一组中选择多个标签时采用“或”逻辑。
- 核心标签组与关联标签组之间采用“且”逻辑。
- 基金代码条件与核心标签组、关联标签组之间采用“且”逻辑。
- 页面提供“一键清空全部条件”。
- 被选标签以填充色、边框和勾选图标共同标识，不能只依赖颜色。
- 当前筛选条件以可关闭的条件标签展示。
- “一键清空全部条件”同时清除基金代码、二级分类、核心标签和关联标签条件，并恢复全量基金结果。

逻辑示例：

> 核心标签选择“科技金融、绿色金融”，关联标签选择“数字金融”，表示筛选“核心标签为科技金融或绿色金融，且关联标签包含数字金融”的基金。

### 5.7 基金结果表

首版仅展示以下字段，不增加额外列：

| 列名 | 字段 | 说明 |
| --- | --- | --- |
| 基金代码 | `fundCode` | 保留前导零，作为字符串处理 |
| 基金简称 | `fundName` | 单行展示，过长时省略并提供悬浮完整值 |
| 二级分类 | `secondaryCategory` | 展示正式数据中的二级分类名称，与筛选器口径一致 |
| 基金规模（亿元） | `fundScale` | 有效数值固定保留两位小数；缺失时显示 `—` |
| 核心标签及占比 | `coreLabels` | 标签名称与对应百分比成组展示；当前可能有一个或两个核心标签，无核心时显示“暂无核心标签” |
| 关联标签及占比 | `relatedLabels` | 标签与百分比成组展示 |

表格交互：

- “基金代码”和“基金简称”均以链接形式展示，并指向同一基金详情路由 `/funds/:fundCode`。
- 点击基金代码或基金简称均可进入对应基金详情页。
- 链接需具有明确的链接视觉状态，包括主题色文字和悬浮/键盘聚焦状态；不能只通过整行底色暗示可点击。
- 仅基金代码与基金简称可点击，表格行本身及核心标签、占比、关联标签等其他单元格均不可点击。
- 链接应使用 React Router 的 `Link` 或等效的语义化链接实现，支持鼠标点击、Tab 键聚焦和 Enter 键进入。
- 默认按最大标签占比从高到低排序；相同时按基金代码升序。
- 每页默认 20 条，可切换 20/50/100 条。
- 显示当前结果总数。
- 无结果时保留筛选条件并提示“未找到符合当前条件的基金”。
- 基金规模使用统一数值格式化函数，列表单元格只显示数值，如 `12.34`；单位只在列名“基金规模（亿元）”中出现，避免逐行重复。

标签与占比展示规则：

- `coreLabels` 和 `relatedLabels` 均使用同一个标签占比列表组件，输入类型统一为 `Array<{ label, ratio }>`。
- 每一项同时显示标签名称和格式化百分比，例如 `科技金融 58.0%`。
- 核心标签使用较高视觉强调，关联标签使用次级强调，但排序、换行和百分比格式化逻辑必须复用。
- 多个标签在同一单元格内横向排列，空间不足时自动换行，不新增“核心标签1”“核心标签2”等固定页面列。
- 同一集合内优先按占比从高到低排列；占比相同时按科技、绿色、普惠、养老、数字的固定顺序排列。

### 5.8 状态处理

- 加载中：表格骨架屏。
- 加载失败：显示错误提示与“重新加载”按钮。
- 数据为空：提示“当前数据文件中暂无基金记录”。
- 筛选无结果：提示“未找到符合当前条件的基金”，并提供“清空筛选”。

## 6. 页面二：基金详情页

### 6.1 页面目标

以一个页面完整呈现单只基金的基本信息、核心/关联标签和五维占比画像。

### 6.2 返回行为

- 页面左上提供“返回基金池”。
- 从基金列表的代码或简称链接进入详情页时，详情页必须立即定位到页面顶部，使用无动画的 `scrollTo({ top: 0, behavior: "auto" })`，不得继承基金池页的滚动位置。
- 进入详情页前应记录基金池状态，包括基金代码输入与解析结果、二级分类筛选、核心/关联标签筛选、页码和列表滚动位置。
- 点击“返回基金池”时恢复上述基金池状态，并在列表完成渲染后恢复原滚动位置。
- 在详情页搜索并切换另一只基金时，新基金详情同样立即定位到页面顶部，同时继续保留最初进入详情页时的基金池来源状态。
- 用户直接访问或刷新详情 URL `/funds/:fundCode`，且不存在来源页面状态时，“返回基金池”进入默认基金池页 `/funds`。
- 浏览器后退键与页面内“返回基金池”按钮应采用一致的恢复逻辑。
- 推荐在跳转时通过 React Router `location.state` 保存 `returnTo`、基金代码输入与匹配结果、二级分类筛选、标签筛选、页码和滚动位置；详情页无有效来源状态时回退到 `/funds`。

### 6.3 单只基金搜索与切换

- 详情页顶部单独设置“切换基金”搜索栏，位于返回入口与基金基本信息之间或同一首屏顶部区域。
- 仅支持输入一个完整基金代码并进行精确匹配；不支持基金简称、多个代码、代码模糊搜索、搜索联想或自动补全 `.OF`。
- 用户点击“查看”按钮或按 Enter 后，对输入值执行 `trim().toUpperCase()`，并从全量基金池中按 `fundCode` 精确查找。
- 匹配成功后使用 `replace: true` 导航至 `/funds/:fundCode`，更新基金基本信息、标签摘要、雷达图和比例条形图，并将详情页定位到顶部；详情页连续切换基金不在浏览器历史中堆积多个详情记录。
- 切换基金时沿用原有 `returnTo` 与基金池来源状态，避免返回时丢失基金池筛选条件。
- 匹配失败时不改变当前基金详情，在搜索栏附近显示：`未找到基金代码 AAA.OF，请检查输入是否正确。`
- 搜索输入为空时不导航，并提示“请输入完整基金代码”。

输入提示文案：`输入完整基金代码，如 000001.OF`

### 6.4 基金基本信息卡片

展示字段：

- 基金代码
- 基金简称
- 基金类型：由“一级分类”和“二级分类”共同构成，优先显示二级分类，可用“一级分类 · 二级分类”补充层级关系
- 基金管理人或基金公司
- 基金规模：显示为 `12.34亿元`；有效数值固定保留两位小数，缺失时显示 `—`

卡片应位于详情页首屏顶部，基金名称为主要标题，基金代码为标题下方的次级信息。

基本信息布局规则：

- 标题区下方的基金代码、基金类型、基金管理人或基金公司、基金规模四项信息，在桌面端同一行展示，不得将基金规模单独放置到下一行。
- 四项信息采用12列不等宽网格，推荐列宽比例为 `2 : 4 : 4 : 2`，即基金代码占2列、基金类型占4列、基金管理人占4列、基金规模占2列。
- 四项的字段名称与字段值均顶部对齐；字段名称使用次级灰色文字，字段值使用主文字颜色。
- 基金类型和基金管理人允许自然换行，但不得挤压基金代码或基金规模，也不得使用过小字号强行保持单行。
- 当可用内容宽度低于1280px时，四项自动调整为两列两行；不得在窄屏上强制维持四列导致内容重叠。
- 基本信息卡片高度随内容自适应，并减少标题区与信息区之间的多余留白，使后续标签摘要和图表更靠近首屏。
- 基金名称及其下方的次级基金代码属于标题区，不计入上述四项信息网格。

### 6.5 核心与关联标签摘要

- 核心标签使用高强调样式，并显示对应占比。
- 关联标签使用次级样式，并显示对应占比。
- 无核心标签时显示中性提示“暂无核心标签”。
- 无关联标签时显示中性提示“暂无关联标签”。

### 6.6 五维标签雷达图

固定五个维度：科技金融、绿色金融、普惠金融、养老金融、数字金融。

图表规则：

- 五个轴统一使用 0–100% 标度。
- 所有基金使用相同量纲和轴顺序，保证跨基金视觉可比性。
- 悬浮提示展示标签全称和准确百分比。
- 使用单一主色半透明面积填充，避免五种颜色叠加降低可读性。
- 五类占比字段缺失时，数据转换层统一将其归一化为 0；雷达图按 0 正常绘制，对应维度落在中心点。
- 图表动画控制在 200–400ms，避免汇报场景中产生过度动效。

### 6.7 五类标签占比条形图

采用横向比例条形图，不使用饼图或环形图。

理由：五类标签是相互独立的维度，同一基金可以同时覆盖多个标签，五类占比不要求合计为100%。饼图隐含“各部分共同构成一个整体”，会造成口径误读。

图表规则：

- 纵轴固定为五个正式标签，顺序与雷达图一致。
- 横轴为 0–100%。
- 每条末端显示精确百分比。
- 使用五类固定颜色，但同时显示文本标签和数值，不能只靠颜色编码。
- 核心标签对应条形可增加强调描边；关联标签保持常规强调；低于20%的标签仍展示数值。
- 五类占比字段缺失时，对应条形按 0 绘制并显示 `0.0%`。
- 图表支持悬浮提示。

### 6.8 基金不存在

当 URL 中的基金代码在数据文件中不存在时，展示独立 404 状态：

- 标题：“未找到该基金”
- 展示请求的基金代码
- 提供“返回基金池”按钮

## 7. 筛选逻辑定义

设：

- `S`：基金池页已解析并去重的基金代码集合
- `C`：已选核心标签集合
- `R`：已选关联标签集合
- `K`：已选二级分类集合
- `fund.coreLabels`：某基金的核心标签集合
- `fund.relatedLabels`：某基金的关联标签集合

基金进入结果集的条件为：

```text
(S为空 或 fund.fundCode 属于 S)
且
(K为空 或 fund.secondaryCategory 属于 K)
且
(C为空 或 fund.coreLabels 与 C 存在交集)
且
(R为空 或 fund.relatedLabels 与 R 存在交集)
```

基金代码集合内部采用 OR，即输入任一基金代码均可进入候选子集；二级分类组、核心标签组和关联标签组内部同样采用 OR。基金代码集合、二级分类组、核心标签组和关联标签组之间采用 AND。

基金池页批量代码解析逻辑为：

```text
tokens = input.split(逗号、分号、空格或换行)
S = tokens.map(trim与大写转换).filter(非空).去重()
matchedCodes = S 与全量fundCode集合的交集
unmatchedCodes = S 减去全量fundCode集合
```

部分代码未匹配时，仍使用 `matchedCodes` 参与列表筛选，并单独展示 `unmatchedCodes`。若所有输入代码均未匹配，结果表显示空状态及全部未匹配代码。

详情页单只基金搜索独立于基金池筛选，其逻辑为：

```text
normalizedCode = input.trim().toUpperCase()
targetFund = allFunds.find(fund => fund.fundCode === normalizedCode)

若 targetFund 存在：保留来源状态并导航至 /funds/:fundCode
若 targetFund 不存在：保留当前详情并显示输入级错误提示
```

其中详情页搜索必须基于尚未应用任何列表筛选的全量基金池。

## 8. 数据规范

### 8.1 正式数据源

- 唯一正式数据源为项目根目录下的 `fund_labels_web_cleaned.xlsx`。
- 构建脚本不得继续读取 `样例数据_已填充(1).xlsx`、`funds.xlsx` 或其他历史样例文件。
- 页面所需的必填字段以 8.3 的字段映射表为准；源表如包含预计算的核心/关联标签字段，仅用于一致性核验，不作为页面直接读取的权威结果。
- 正式文件允许包含其他冗余列；未映射列按 8.4 的安全边界忽略。
- Excel 工作表名称通过 `dataSheetName` 配置，默认读取 `Sheet1`。如正式文件工作表名称不同，只修改该配置，不修改解析组件。
- 五类占比继续采用 0–1 的底层数值口径，例如底层值 `0.105` 在页面显示为 `10.5%`。
- PRD 不固定正式文件的记录数，也不假设预计算标签采用固定数量的列槽位；记录数由每次构建自动生成。

### 8.2 首版数据接入方式

- 数据更新方式正式定义为“人工替换数据源的离线批量更新”，属于基于文件的构建时全量更新，不是网页上传、数据库实时更新、API自动同步或增量更新。
- 构建阶段将项目根目录下的 `fund_labels_web_cleaned.xlsx` 转换为标准 JSON。
- 正式数据暂定每季度更新一次，由项目维护人员人工提供并替换经过确认的 Excel 文件。
- 新一期数据必须覆盖项目根目录下的同名文件 `fund_labels_web_cleaned.xlsx`；不得通过修改前端组件或另建一份并行 JSON 完成更新。
- 网页不面向普通用户提供 Excel 上传入口，也不在浏览器运行时直接解析 Excel。
- Excel 工作表名称通过配置项 `dataSheetName` 指定；首版默认读取 `Sheet1`。
- 默认第一行是字段名，并忽略全空的尾部行与尾部列。
- 解析器按规范化后的字段名匹配：去除字段名前后空格与 UTF-8 BOM，但不得按列序号读取。
- 字段顺序可以变化；新增未知字段不得导致导入失败。

季度数据更新流程：

```text
fund_labels_web_cleaned.xlsx
→ 更新数据统计截止日期（asOfDate）
→ 字段名映射
→ 数值及唯一性校验
→ 重新计算核心标签与关联标签
→ 与源表预计算标签核验
→ 输出 public/data/funds.normalized.json
→ 构建并加载新版静态网页
```

更新规则：

1. `fund_labels_web_cleaned.xlsx` 是人工维护的唯一原始数据文件，`funds.normalized.json` 是网页实际读取的数据文件。
2. `asOfDate` 在单一数据配置项中维护，当前值为 `2026-09-30`；不得在基金池页和详情页分别维护日期文案。
3. 更新脚本通过 `npm run update-data` 统一完成字段名映射、五类占比规范化、缺失占比转 0、标签计算、源表标签核验、元数据写入和 JSON 输出。
4. 正式构建命令统一为 `npm run build`，通过 `prebuild` 自动先运行数据更新脚本，再执行 Vite 构建；维护人员无需依次执行多条命令。
5. 不设置“硬错误”“软错误”等多级错误分类，仅在生成 JSON 前执行必要的完整性检查。
6. 检查未通过时不生成或覆盖新的 JSON，上一期有效数据保持不变，同时终止本次网页构建。
7. 检查通过后输出基金记录数量和转换完成信息，再继续生成 `dist/` 静态网站目录。

维护人员的标准操作仅包括：

1. 用新一期 Excel 覆盖项目根目录下的 `fund_labels_web_cleaned.xlsx`。
2. 更新单一 `asOfDate` 配置。
3. 在项目根目录执行一次 `npm run build`。
4. 构建成功后，用新生成的 `dist/` 目录替换上一版本静态网站。

“重新运行脚本即可同步”是指人工触发上述全量转换与重新构建流程，不表示网页自动监测 Excel 文件，也不表示数据实时同步。

### 8.3 中文源字段与内部字段映射

| Excel字段 | 内部字段 | 类型 | 必填 | 页面用途 |
| --- | --- | --- | --- | --- |
| `基金代码` | `fundCode` | string | 是 | 搜索、主键、列表与详情展示；保留前导零和`.OF`后缀 |
| `基金简称` | `fundName` | string | 是 | 列表与详情展示 |
| `管理人` | `manager` | string | 是 | 详情页基本信息 |
| `一级分类` | `primaryCategory` | string | 是 | 基金类型层级信息 |
| `二级分类` | `secondaryCategory` | string | 是 | 列表展示、二级分类筛选及详情页基金类型的主要显示值 |
| `基金规模（亿元）` | `fundScale` | number \| null | 是（列必填） | 列表与详情页展示；单元格允许为空并转为 `null` |
| `科技金融占比` | `technologyFinanceRatio` | number | 是 | 标签计算、雷达图、条形图；输入缺失时转换为0 |
| `绿色金融占比` | `greenFinanceRatio` | number | 是 | 同上 |
| `普惠金融占比` | `inclusiveFinanceRatio` | number | 是 | 同上 |
| `养老金融占比` | `pensionFinanceRatio` | number | 是 | 同上 |
| `数字金融占比` | `digitalFinanceRatio` | number | 是 | 同上 |

Excel 输入中的五类占比允许为空；转换后内部占比始终保存为 0–1 的非空比率值，空白、`NA`、`N/A`和`--`统一转换为 0。界面显示时再格式化为百分比。不得把 `0.5` 显示为 `0.5%`，也不得在导入时将其再次除以 100。

基金规模字段规则：

- Excel 表头必须为 `基金规模（亿元）`，单位已经确定为亿元；不得在转换时再次进行万元、亿元之间的换算。
- 有效值解析为非负有限数值；空白、`NA`、`N/A`和`--`统一转为 `null`。
- 无法解析的非空文本或负数属于数据校验错误，应输出基金代码及原始值并终止构建。
- 标准 JSON 保留原始数值精度，页面展示层统一保留两位小数；不得在数据转换阶段提前将数值改写为字符串。

正式 Excel `fund_labels_web_cleaned.xlsx` 采用 0–1 数值口径，不允许同一文件混用 0–1 与 0–100 两种占比口径。

### 8.4 可选字段、派生字段与冗余列

| Excel字段 | 处理方式 | 首版用途 |
| --- | --- | --- |
| `股票持仓` | 解析为0–1可选比率并保留 | 暂不展示 |
| 源表预计算的核心标签及占比字段（如存在） | 规范化为可选数组后保留 | 仅用于与重新计算结果做一致性校验，不限定核心标签数量 |
| 源表预计算的关联标签及占比字段（如存在） | 规范化为可选数组后保留 | 仅用于一致性校验，不作为网页标签数量上限 |
| 其他未知列 | 在字段映射阶段忽略 | 不进入标准JSON、组件属性或页面DOM，不参与搜索、筛选和标签计算，不阻塞导入 |

实现要求：

1. 页面功能只依赖 8.3 中的必填字段。
2. 源表新增“基金经理、成立日期、风险等级、内部备注”等列时，现有页面应正常加载。
3. 可选列缺失时不报错；必填列缺失时停止加载，并明确列出缺失字段。
4. 不得因为存在未知列、列顺序变化或预计算标签列为空而判定整份文件无效。
5. 未映射字段不得写入标准 JSON，不得进入任何页面组件的属性，也不得写入页面 DOM。
6. 未映射字段不得参与基金代码匹配、标签筛选、排序、图表或任何前端计算，避免内部辅助字段或敏感字段意外公开。

### 8.5 内部数据对象建议

```ts
type FundRecord = {
  fundCode: string;
  fundName: string;
  manager: string;
  primaryCategory: string;
  secondaryCategory: string;
  technologyFinanceRatio: number;
  greenFinanceRatio: number;
  inclusiveFinanceRatio: number;
  pensionFinanceRatio: number;
  digitalFinanceRatio: number;
  fundScale: number | null;
  stockHoldingRatio?: number | null;
  sourceCoreLabels?: Array<{ label: FinanceTag; ratio: number }>;
  sourceRelatedLabels?: Array<{ label: FinanceTag; ratio: number }>;
  coreLabels: Array<{ label: FinanceTag; ratio: number }>;
  relatedLabels: Array<{ label: FinanceTag; ratio: number }>;
};

type FundDataPayload = {
  meta: {
    asOfDate: string;       // ISO日期，当前为2026-09-30
    generatedAt: string;    // JSON生成时间，ISO 8601格式
    recordCount: number;
    ruleVersion: string;
  };
  funds: FundRecord[];
};
```

其中 `coreLabels` 和 `relatedLabels` 必须由五类原始占比重新计算，并统一采用动态数组结构。正式数据中可能出现一个或两个核心标签，代码不得固定为“核心标签1/核心标签2”，也不得以源表预计算标签列的数量限制页面可展示的标签数。

### 8.6 标准JSON输出示例

```json
{
  "meta": {
    "asOfDate": "2026-09-30",
    "generatedAt": "2026-09-30T18:00:00+08:00",
    "recordCount": 1,
    "ruleVersion": "1.0"
  },
  "funds": [
    {
      "fundCode": "000001.OF",
      "fundName": "示例成长增强",
      "manager": "示例基金管理有限公司",
      "primaryCategory": "混合型基金",
      "secondaryCategory": "偏股混合型基金",
      "fundScale": 12.34,
      "technologyFinanceRatio": 0.58,
      "greenFinanceRatio": 0.24,
      "inclusiveFinanceRatio": 0.08,
      "pensionFinanceRatio": 0.05,
      "digitalFinanceRatio": 0.52,
      "coreLabels": [
        { "label": "科技金融", "ratio": 0.58 },
        { "label": "数字金融", "ratio": 0.52 }
      ],
      "relatedLabels": [{ "label": "绿色金融", "ratio": 0.24 }]
    }
  ]
}
```

元数据规则：

- `asOfDate` 是数据统计截止日期，当前固定为 `2026-09-30`；页面统一格式化显示为“截至2026年9月30日”。
- 后续季度更新 Excel 时，维护人员必须同步更新 `asOfDate`，再执行构建命令。
- `recordCount` 由转换脚本根据有效基金记录自动生成，不得人工填写。
- `generatedAt` 由转换脚本自动生成，仅用于数据追溯，首版页面无需展示。
- `ruleVersion` 用于标记核心/关联标签阈值规则版本，首版为 `1.0`。

### 8.7 更新前完整性检查

本项目暂不设置“硬错误”和“软错误”分类。每季度人工替换 Excel 后，更新脚本在生成标准 JSON 前统一检查：

1. 必填字段是否存在；同名必填字段不得重复。
2. `基金代码`是否为空或重复，并始终将其作为字符串处理。
3. 五类占比在完成缺失值转 0 后是否均为 0–1 之间的有效数值。
4. `基金规模（亿元）`列是否存在；非空规模是否为大于等于0的有限数值。
5. `二级分类`是否为非空文本，并在去除前后空格后写入标准 JSON。
6. 由五类占比分别重新计算核心/关联标签；任一达到阈值的标签均进入对应数组，不强制只保留一个核心标签。

检查未通过时终止本次转换，不覆盖上一期 `funds.normalized.json`。检查通过后生成新 JSON。文本字段的前后空格由数据转换层统一清除。五类标签为相互独立的研究维度，不校验合计值；合计小于、等于或大于 1 均可正常生成数据。

## 9. 视觉与交互规范

### 9.1 设计方向

关键词：简洁、现代、克制、专业、数据分析。

- 以浅灰白背景和白色卡片构建层级。
- 主色建议使用深蓝或蓝绿色，避免高饱和渐变和装饰性背景。
- 通过留白、字重、边框和轻微阴影组织层级。
- 不使用大面积深色表格底纹；表头可使用浅色背景配深色文字。
- 避免复杂三维图表、玻璃拟态和无功能动效。

基础视觉变量统一在一处定义，页面组件不得重复写入相同色值：

| 变量 | 建议值 | 用途 |
| --- | --- | --- |
| 页面背景 | `#F6F8FB` | 页面底色 |
| 卡片背景 | `#FFFFFF` | 指标、筛选、表格和图表容器 |
| 主文字 | `#111827` | 标题及正文 |
| 次级文字 | `#6B7280` | 单位、统计日期及辅助信息 |
| 边框 | `#E5E7EB` | 卡片、输入框及表格分隔 |
| 主交互色 | `#1D4ED8` | 按钮、链接及聚焦状态 |
| 卡片圆角 | `10px` | 全站卡片统一圆角 |
| 内容最大宽度 | `1440px` | 桌面与大屏内容区 |

### 9.2 标签颜色建议

| 标签 | 建议色彩语义 | 示例色值 |
| --- | --- | --- |
| 科技金融 | 稳健蓝 | `#2563EB` |
| 绿色金融 | 绿色 | `#16A34A` |
| 普惠金融 | 琥珀色 | `#D97706` |
| 养老金融 | 紫色 | `#7C3AED` |
| 数字金融 | 青色 | `#0891B2` |

颜色只作为辅助编码，必须同时显示标签文字和百分比。

### 9.3 字体与数字

- 中文优先：`PingFang SC`、`Microsoft YaHei`、系统无衬线字体。
- 百分比、基金规模与基金代码使用等宽数字或 `font-variant-numeric: tabular-nums`。
- 基础字号不低于 14px；表格正文建议 14–16px；大屏标题可适度放大。

### 9.4 页面布局基线

基金池页：

- 页面左右内边距在桌面端为 32px，区域之间的垂直间距统一采用 24px。
- 顶部身份区左侧显示项目正式名称 `基金池“五篇大文章”标记体系`，右侧显示“中银理财-基海淘金队”和“截至2026年9月30日”。
- 概览区采用12列网格：左侧4列放置2×2指标卡，右侧8列放置五类标签覆盖基金数横向条形图。
- 单个指标卡建议高度约100px；概览条形图容器建议高度260px。
- 基金代码筛选输入区建议宽度560–720px，并支持随换行适度增高，以便粘贴多行基金代码；二级分类使用多选下拉框，核心标签和关联标签分别占一行。
- 表格表头高度约44px，数据行高度约52–56px。
- 基金代码和基金简称使用主交互色；悬浮时显示下划线，键盘聚焦时显示清晰轮廓。

基金详情页：

- 顶部依次显示返回入口、“切换基金”搜索栏、基金名称与代码、基金基本信息及核心/关联标签摘要。
- 基本信息卡片在1280px及以上采用四项单行布局，按基金代码、基金类型、基金管理人、基金规模的顺序排列，推荐宽度比例为 `2 : 4 : 4 : 2`；低于1280px时改为两列两行。
- 图表区采用12列网格：左侧5列为雷达图，右侧7列为横向比例条形图。
- 两张详情图表的容器高度统一为400–440px。
- 当可用宽度低于1280px时，两张图表改为上下排列，不缩小到影响文字阅读。

### 9.5 屏幕适配

- 桌面优先，重点验收 1440×900、1920×1080。
- 浏览器范围至少覆盖验收时最新版 Google Chrome 和 Microsoft Edge。
- 会议室电脑需验证浏览器 100%、110%、125% 三种常用缩放比例。
- 建议内容最大宽度 1440px；在 1920px 大屏保持居中，不无限拉伸表格。
- 1280px 及以上不得出现页面级横向滚动条。
- 低于 1280px 时允许表格区域内部横向滚动，但首版不针对手机重新设计。

### 9.6 动效

- 按钮、标签、表格行：150–200ms 状态过渡。
- 图表首次加载：200–400ms。
- 尊重系统 `prefers-reduced-motion` 设置。

## 10. 技术实现建议（非强制）

### 10.1 已确认技术方案

- React + TypeScript + Vite
- Tailwind CSS
- 原生语义化 HTML `table` 配合 React `useMemo` 实现固定列、筛选、排序与分页；首版不引入 TanStack Table
- Apache ECharts 实现雷达图与横向条形图
- React Router `HashRouter` 管理基金池页与详情页
- Zod 规范化并校验 Excel 转换后的数据对象
- 独立更新脚本完成 Excel → 标准 JSON 转换
- Vitest 进行标签规则和数据转换单元测试
- Playwright 进行基金池筛选、详情跳转和大屏布局端到端测试

首版应为纯前端静态单页应用，不需要后端服务。`npm run build` 最终输出可独立部署的 `dist/` 目录。所有运行依赖、字体回退、图表代码和基金 JSON 均随构建产物本地提供，不依赖外部 CDN，保证会议室网络不可用时仍可正常展示。标签计算应封装为独立纯函数，并配套单元测试。

路由实现补充：

- 列表中的基金代码与基金简称使用同一个 `Link` 目标 `/funds/:fundCode`，由 `HashRouter` 输出 `/#/funds/:fundCode` 地址。
- 基金池页的单个/批量基金代码输入只更新列表筛选状态，不触发路由跳转。
- 详情页“切换基金”搜索命中后使用 `replace: true` 程序化导航进入新的 `/funds/:fundCode` 路由，并继续传递原基金池来源状态。
- 从基金列表进入详情页时，通过 `location.state` 或等效机制携带基金池来源状态；直接访问详情 URL 时不得依赖该状态才能正常渲染。
- 详情页在 `fundCode` 路由参数变化后使用无动画方式将页面定位到顶部；返回基金池时应在列表渲染完成后恢复来源状态中的滚动位置。
- 不要求静态托管服务器配置 SPA 路由回退；在 `/#/funds/:fundCode` 刷新时仍应正常打开对应基金详情页。

### 10.2 GitHub参考组合

| 参考用途 | 项目 | 使用边界 |
| --- | --- | --- |
| 基金筛选业务结构 | [PengchuanC/fund_vue3](https://github.com/PengchuanC/fund_vue3) | 参考筛选条件、结果表格、基金池与单基金详情的信息架构；不直接复制代码 |
| 中文基金页面与视觉表达 | [dazzlejc/fund-radar](https://github.com/dazzlejc/fund-radar) | 参考中文基金搜索、卡片层级和静态展示；其许可证为CC BY-NC 4.0，不直接用于商业代码 |
| 页面结构、路由与状态管理 | [Kiranism/tanstack-start-dashboard](https://github.com/Kiranism/tanstack-start-dashboard) | 参考页面骨架、URL状态、搜索筛选和分页模式 |
| 多标签筛选与结果表格 | [openstatusHQ/data-table-filters](https://github.com/openstatusHQ/data-table-filters) | 参考多选筛选、条件标签、表格状态和行详情交互 |
| 雷达图与横向条形图 | [Apache ECharts Examples](https://github.com/apache/echarts-examples) | 参考图表配置、tooltip、响应式尺寸和大屏展示 |

上述项目作为分层参考，不以任何单一仓库作为完整模板。产品结构和视觉参考需重新设计；复用开源代码时必须核对并遵守对应许可证，保留必要的许可证声明。

### 10.3 建议目录

```text
fund_labels_web_cleaned.xlsx
data/
└── data-config.ts
scripts/
└── update-data.ts
public/
└── data/
    └── funds.normalized.json
src/
├── components/
│   ├── FundPoolOverview.tsx
│   ├── TagCoverageChart.tsx
│   ├── FundCodeFilter.tsx
│   ├── FundSwitcher.tsx
│   ├── SecondaryCategoryFilter.tsx
│   ├── TagFilters.tsx
│   ├── TagRatioList.tsx
│   ├── FundTable.tsx
│   ├── FundInfo.tsx
│   ├── FundRadarChart.tsx
│   ├── FundRatioChart.tsx
│   └── Pagination.tsx
├── pages/
│   ├── FundPoolPage.tsx
│   └── FundDetailPage.tsx
├── domain/
│   ├── tagRules.ts
│   ├── fundSelectors.ts
│   ├── formatters.ts
│   └── fundTypes.ts
├── data/
│   ├── loadFunds.ts
│   └── normalizeFundRow.ts
├── config/
│   ├── financeTags.ts
│   └── project.ts
└── hooks/
    └── useEChart.ts
tests/
├── unit/
└── e2e/
```

### 10.4 代码精简与单一数据源约束

1. 五类标签的字段名、显示名称、顺序和颜色只在 `financeTags.ts` 定义一次；筛选器、概览图、雷达图和比例条形图统一读取该配置。
2. 核心标签与关联标签阈值只在 `tagRules.ts` 定义和计算一次，页面组件不得重复编写阈值判断。
3. 核心标签及占比、关联标签及占比统一由 `TagRatioList.tsx` 渲染；两处共享数组输入、排序、百分比格式化和换行逻辑，只通过视觉变体区分强调层级。
4. `funds.normalized.json` 在应用启动时只加载一次，并基于同一份数据生成 `funds` 数组和按 `fundCode` 索引的 `Map`；基金详情页不得再次加载第二份数据。
5. 基金池筛选、排序、概览统计等派生结果集中放在 `fundSelectors.ts`，避免在多个组件内分别实现相同计算。
6. 二级分类选项从同一份 `funds` 数据中去重生成，筛选组件只消费选项和选择状态，不保存第二份分类清单。
7. 基金规模格式化统一由 `formatters.ts` 中的 `formatFundScale` 实现；列表和详情页不得分别调用 `toFixed` 或维护不同的缺失值文案。
8. 多基金代码的分隔符解析、规范化、去重和匹配逻辑集中为一个纯函数，基金池筛选组件不得分别实现多套解析规则。
9. ECharts 的初始化、`ResizeObserver`、`resize` 和销毁逻辑统一封装在 `useEChart.ts`，三个图表组件不得复制生命周期代码。
10. 首版不引入 Redux、Zustand、TanStack Query、TanStack Table、通用后台模板或大型组件库。
11. 仅为稳定业务模块或确实重复使用的视觉单元创建组件，不为单个标题、百分比文本或一次性容器创建无业务含义的包装组件。
12. 不复制保存多份业务数据，不在 TypeScript 文件中再次硬编码 JSON 数据。
13. 不使用 `any` 绕过类型检查；数据入口完成一次 Zod 校验后，内部统一消费 `FundDataPayload`。
14. 不实现 PRD 未要求的登录、后台、数据库、主题切换、国际化、导出、收藏、动画背景或移动端独立布局。
15. 相同颜色、圆角、间距和字号必须通过 Tailwind主题或统一CSS变量引用，不得在不同组件重复散落魔法值。
16. 优先使用简单纯函数和局部状态，不进行超出当前数据规模和功能需求的抽象、缓存或性能优化。

## 11. 埋点建议（可选）

若后续用于公开展示，可记录匿名事件：

- 执行基金池单个/批量基金代码筛选
- 在详情页搜索并切换基金
- 选择/取消核心标签
- 选择/取消关联标签
- 选择/取消二级分类
- 清空筛选
- 进入基金详情页
- 从详情页返回基金池

首版原型可不接入埋点服务。

## 12. 验收标准

### 12.1 数据与规则

- [ ] 人工替换 Excel、更新单一 `asOfDate` 配置并执行 `npm run build` 后，可自动生成新的 `funds.normalized.json` 和 `dist/` 静态网站，无需修改页面组件。
- [ ] 使用新一期 Excel 覆盖项目根目录下的 `fund_labels_web_cleaned.xlsx` 后，无需修改源代码或另建并行数据文件。
- [ ] `npm run build` 能依次完成数据转换、必要校验、标准 JSON 输出和 Vite 静态构建；任何必要校验失败时终止构建且不覆盖上一期有效 JSON。
- [ ] 标准 JSON 的 `meta.asOfDate` 为 `2026-09-30`，基金池页与详情页均显示“截至2026年9月30日”。
- [ ] `meta.recordCount` 与实际有效基金记录数量一致。
- [ ] 占比 ≥50% 正确识别为核心标签。
- [ ] 占比 ≥20%且<50% 正确识别为关联标签。
- [ ] 占比 <20% 不进入摘要，但在两类图表中正常展示。
- [ ] 五类占比中的空白、`NA`、`N/A`和`--`均转换为0，并在雷达图和条形图中显示为0。
- [ ] `0.5` 能正确显示为 `50.0%`，并识别为核心标签。
- [ ] 五类占比分别在0–1范围内时可通过校验，任一占比超出范围时能定位错误基金及字段。
- [ ] 五类占比合计大于1时仍可正常构建，不得因总和超过100%报错。
- [ ] 同一基金有一个或两个占比达到50%时，均能生成完整的 `coreLabels` 数组并正确展示，不强制只保留一个核心标签。
- [ ] 列顺序变化后，字段仍能按表头正确映射。
- [ ] 增加任意冗余列后，页面仍能正常加载；未映射字段不进入标准JSON、组件属性、页面DOM、搜索或筛选。
- [ ] 缺少任一必填字段时，错误信息能明确指出缺失的中文字段名。
- [ ] 源表预计算标签与占比规则冲突时，页面采用重新计算结果并记录冲突。
- [ ] `基金规模（亿元）`能够映射为 `fundScale` 数值；空白规模转换为 `null`，负数或无法解析的非空文本能够定位到具体基金并终止构建。
- [ ] 标准 JSON 中基金规模保持数值或 `null`，不因“两位小数展示”被转换成字符串。

### 12.2 搜索与筛选

- [ ] 基金池页输入一个完整基金代码并执行筛选后，页面不跳转，匹配基金在下方结果表中展示。
- [ ] 基金池页可一次输入多个基金代码，并正确识别英文/中文逗号、英文/中文分号、空格和换行分隔符。
- [ ] 基金代码输入区普通 Enter 可输入换行，点击“筛选”或按 Ctrl/Command+Enter 执行筛选。
- [ ] 基金代码在匹配前能正确去除首尾空格、转换为大写、移除空项并去重。
- [ ] 部分代码不存在时，已匹配基金仍正常展示，未匹配代码单独列出。
- [ ] 全部输入代码均不存在时，结果表显示空状态和全部未匹配代码。
- [ ] 二级分类筛选项根据全量数据动态去重生成，并按中文名称升序排列。
- [ ] 二级分类支持多选；组内按 OR，基金代码、二级分类、核心标签组、关联标签组之间按 AND 执行。
- [ ] 清除代码条件后，恢复为仅受当前二级分类和标签条件约束的基金列表。
- [ ] 基金简称、部分代码和其他文本不会触发模糊匹配或搜索联想，系统不会自动补全 `.OF` 后缀。
- [ ] 核心标签和关联标签均可多选。
- [ ] 二级分类、核心标签和关联标签各组内部多选按 OR、跨组按 AND 执行。
- [ ] 清空按钮可一次恢复全部基金结果。
- [ ] 二级分类或标签筛选无结果时显示明确空状态。
- [ ] 详情页“切换基金”搜索栏仅接受一个完整基金代码；命中后更新当前基金全部详情内容。
- [ ] 详情页搜索不存在或为空的代码时不改变当前基金，并显示明确错误提示。

### 12.3 列表与详情

- [ ] 基金池页筛选器上方展示四项全量基金指标和五类标签覆盖基金数横向条形图。
- [ ] “具有核心标签”“仅有关联标签”“暂无核心或关联标签”三项基金数之和等于基金总数。
- [ ] “暂无核心或关联标签的基金数”指标卡在数值下方常驻显示灰色备注“纯债型基金不涉及相关标签”，且备注不改变指标统计结果。
- [ ] 五类标签覆盖图分别展示核心覆盖数和关联覆盖数，并正确计算总覆盖数。
- [ ] 执行基金代码、二级分类或标签筛选后，基金池概览区仍保持全量口径不变。
- [ ] 基金池表格只展示基金代码、基金简称、二级分类、基金规模（亿元）、核心标签及占比、关联标签及占比，不展示股票持仓或未知冗余列。
- [ ] 基金规模有效值在列表中固定显示两位小数，如 `12.30`；缺失值显示 `—`。
- [ ] 基金池表格使用单一“核心标签及占比”列，不再分别展示“核心标签”和“核心标签占比”。
- [ ] “核心标签及占比”列能在同一单元格展示一个或两个核心标签及各自占比；空间不足时自动换行。
- [ ] 核心标签与关联标签的列表渲染逻辑复用同一组件，排序和百分比格式一致。
- [ ] 基金代码和基金简称均以可识别、可聚焦的链接形式展示，并进入同一基金详情页。
- [ ] 表格行及代码、简称之外的单元格不可点击。
- [ ] 详情页基本信息卡片展示基金代码、基金简称、基金类型、基金管理人或基金公司、基金规模，并包含标签摘要、雷达图和条形图。
- [ ] 在1440×900和1920×1080下，基金代码、基金类型、基金管理人或基金公司、基金规模四项在同一行展示，排列顺序和宽度比例符合 `2 : 4 : 4 : 2`。
- [ ] 当内容宽度低于1280px时，四项信息自动调整为两列两行，长基金类型或管理人名称不会与相邻字段重叠。
- [ ] 详情页基金规模固定显示两位小数并附带“亿元”单位，如 `12.30亿元`；缺失值显示 `—`。
- [ ] 雷达图与条形图数值均与原始数据一致。
- [ ] 从列表链接进入详情页后，页面立即无动画定位到详情页顶部。
- [ ] 在详情页搜索并切换另一只基金后，页面再次无动画定位到顶部。
- [ ] 从详情页返回基金池，可恢复原基金代码输入与匹配结果、二级分类筛选、标签筛选、页码和列表滚动位置。
- [ ] 在详情页连续切换基金后返回，仍恢复最初进入详情页时的基金池状态。
- [ ] 直接访问或刷新 `/#/funds/:fundCode` 且不存在来源状态时，页面仍可正常加载；点击“返回基金池”可进入默认 `/#/funds` 页面。
- [ ] 浏览器后退键与页面内“返回基金池”按钮的返回结果一致。
- [ ] 不存在的基金代码显示独立未找到状态。

### 12.4 视觉与适配

- [ ] 1440×900 和 1920×1080 下无布局错位。
- [ ] 验收时最新版 Chrome 和 Edge 均能正常加载、筛选和展示图表。
- [ ] 会议室电脑在浏览器100%、110%、125%缩放比例下无关键内容遮挡或错位。
- [ ] 1280px及以上无页面级横向滚动。
- [ ] 基金池概览区在桌面端按4列指标区＋8列图表区呈现，详情页按5列雷达图＋7列条形图区呈现。
- [ ] 大屏展示时表格、标签和图表文字清晰可读。
- [ ] 所有颜色编码同时具备文字或图标辅助。
- [ ] 页面不使用饼图展示五类比例。
- [ ] 页面顶部正确显示项目正式名称 `基金池“五篇大文章”标记体系`。
- [ ] 页面顶部正确显示课题组名称“中银理财-基海淘金队”。
- [ ] 断开外部网络后，已部署的静态网站仍能加载数据、筛选、跳转并绘制图表。

## 13. 待确认项

以下事项不阻碍首版开发，但正式交付前需要确认：

1. 对外公开展示的内容边界与数据脱敏要求。
2. 基金管理信息最终展示“基金管理人”“基金公司”，还是两者择一。
3. 季度数据更新的具体文件负责人。
4. 基金代码是否长期采用带市场后缀格式，如 `.OF`。

## 14. 首版交付清单

1. 基金池页。
2. 基金详情页。
3. 基金池页单个/批量基金代码筛选与未匹配代码提示。
4. 基金详情页单只基金搜索与切换。
5. 基金池四项概览指标和五类标签覆盖基金数横向条形图。
6. 二级分类多选筛选、基金规模列表展示和详情页基金规模展示。
7. 使用 `fund_labels_web_cleaned.xlsx` 中通过校验的全部记录作为页面数据，并另设少量不进入正式页面的自动化边界测试数据。
8. 与正式数据源字段映射一致的数据配置与说明。
9. Excel → `funds.normalized.json` 数据更新脚本及必要完整性检查。
10. 核心/关联标签规则函数及单元测试。
11. 桌面端和大屏适配。
12. 可直接部署的 `dist/` 静态网站目录，所有运行依赖均已本地打包，不使用外部 CDN。
13. README：说明如何替换 Excel、更新 `asOfDate`、执行单一 `npm run build` 命令、启动本地预览和部署 `dist/`。

自动化测试夹具必须覆盖以下情形，测试数据不得混入正式页面数据：

- 单一核心标签。
- 两个标签恰好各50%。
- 两个核心标签占比均高于50%，且五类占比合计超过100%。
- 无核心标签但存在关联标签。
- 无核心也无关联标签。
- 五类合计小于100%。
- 某一占比为20%。
- 某一占比为50%。
- 某一占比缺失并按0处理。
- 基金基本信息字段缺失。
- 基金规模为整数、一位小数、两位以上小数和空白值。
- 基金规模为负数或非空文本，验证构建失败信息。
- 多个二级分类的 OR 筛选及其与标签条件的 AND 组合。
- 列顺序变化。
- 新增一个未识别的冗余列。
- 源表标签结果与五类占比重新计算结果不一致。
